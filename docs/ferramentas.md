# Ferramentas

DETRAN SP: Autenticidade de Certidão expõe 1 ferramenta (todas somente leitura).

### 1. `detran_sp_autenticidade_consultar`
**Input**: `codigo_certidao`, `login_cpf` (opcional), `login_cnpj` (opcional), `login_senha`

DETRAN SP: Autenticidade de Certidão, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar DETRAN SP: Autenticidade de Certidão
```
